# Profile: ${profile.name}
# generated by the fabric8 gradle plugin at ${new Date()}

attribute.parents = ${profile.parent.profiles}

bundle.${packaging}\\:mvn\\:${groupId}/${artifactId}/${version} = ${packaging}\\:mvn\\:${groupId}/${artifactId}/${version}

